apiVersion: v1
kind: Service
metadata:
  name: {{ include "coffee-diary.frontend.fullname" . }}
  labels:
    {{- include "coffee-diary.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coffee-diary.frontend.selectorLabels" . | nindent 4 }}

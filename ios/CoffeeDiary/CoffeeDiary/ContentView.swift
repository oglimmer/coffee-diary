// This file is unused — the app root is in CoffeeDiaryApp.swift.
// Kept to avoid Xcode template warnings; safe to delete.
import SwiftUI

struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
